services:
  web:
    build: .
    container_name: entradas_web
    ports:
      - "8000:8000"
    env_file:
      - .env.docker
    volumes:
      - media_data:/app/media
      - static_data:/app/staticfiles
    restart: unless-stopped

volumes:
  media_data:
  static_data:
